[project]
name = "chaokaimind"
version = "0.1.0"
description = "LLM learning project"
readme = "README.md"
requires-python = ">=3.11,<3.12"

dependencies = [
    "torch==2.7.*",
    "transformers",
    "datasets",
    "numpy",
    "pandas",
    "rich",
    "ruff",
]